on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    name: pandiff pr
    steps:
      - name: Hello world
        id: hello
        uses: carafelix/simple-pandiff-action@220d8a42fffabccff11550cdedaf46b9245e03dc

      # Use the output from the `hello` step
      # - name: Get the output time
      #   run: echo "${{ steps.hello.outputs.diffs }}"
