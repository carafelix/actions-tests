on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    name: pandiff pr
    steps:
      - name: Hello world
        id: hello
        uses: carafelix/simple-pandiff-action@316319ebae30e54c75ee34133303a6afad68b8ac

      # Use the output from the `hello` step
      # - name: Get the output time
      #   run: echo "${{ steps.hello.outputs.diffs }}"
