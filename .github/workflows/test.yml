on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    name: pandiff pr
    steps:
      - name: Hello world
        id: hello
        uses: carafelix/simple-pandiff-action@a40c3f3058ac66eabd7204dbc7ecc61d5f2f3afd

      # Use the output from the `hello` step
      # - name: Get the output time
      #   run: echo "${{ steps.hello.outputs.diffs }}"
