on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    name: pandiff pr
    steps:
      - name: Hello world
        id: hello
        uses: carafelix/simple-pandiff-action@af63c815a6df80457de67f446c77ab9c8aaecfbf

      # Use the output from the `hello` step
      # - name: Get the output time
      #   run: echo "${{ steps.hello.outputs.diffs }}"
